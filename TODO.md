# Stock Opportunity Lab TODO

更新时间：2026-06-03

## 本轮已验证

- [x] `/` 今日机会：可运行盘后扫描，候选池可渲染。
- [x] `查看证据`：按钮已改为 `/?inspect=代码` 原生链接兜底；浏览器验证可打开证据抽屉。
- [x] 证据抽屉：已接入真实分钟行情图，支持分时线 / 蜡烛图与 1/5/15/30/60 分钟切换。
- [x] 分钟行情接口：`/api/intraday` 已按目标交易日过滤，避免把其他日期的分钟线混进“当天分时”。
- [x] `/alerts`、`/sectors`、`/settings`：路由可直达，浏览器巡检无控制台错误。

## P0 - 影响核心可用性

- [ ] 把 `inspect` 证据打开逻辑迁到正式 TanStack Router search params，而不是当前的原生 URL 兜底。
  - 现状：`/?inspect=002645` 能打开抽屉，但这是为了修复点击不稳定先做的可靠兜底。
  - 目标：用 typed search params 管理抽屉状态，关闭抽屉时同步清理 search。

- [ ] 给关键交互补正式 E2E 测试。
  - 覆盖：盘后扫描、板块排除、查看证据、分钟周期切换、直接访问 `/settings` / `/alerts` / `/sectors`。
  - 现状：已用浏览器人工巡检，但没有自动化回归保护。

- [ ] 缓存并恢复上次回测结果。
  - 现状：上次扫描结果已缓存，刷新后候选池可恢复；回测结果刷新后仍为空。
  - 目标：`/backtest` 直接打开时能恢复最近一次回测摘要和表格。

## P1 - 数据真实性与策略质量

- [ ] `/alerts` 接入真实消息源。
  - 现状：页面名是“消息异动”，实际展示的是候选池的成交额、换手、量比规则告警。
  - 目标：接入公告、新闻、龙虎榜或异动公告数据源，并标明来源和时间。

- [ ] `/sectors` 接入全市场板块资金流。
  - 现状：只按候选池聚合交易板块和行业，不代表全市场板块资金强弱。
  - 目标：接入行业 / 概念板块资金流，展示主线强度、资金净流入、候选股覆盖度。

- [ ] 分钟图补充更接近实盘分时的信息。
  - 现状：东方财富分钟 K 已可用，但均价线多数数据源为空。
  - 目标：补均价线、昨收参考线、午间断点处理、hover OHLC tooltip、失败源切换提示。

- [ ] 数据源失败时展示更清楚的降级原因。
  - 现状：接口会 fallback，但前端只看到空态或错误文本。
  - 目标：展示 EastMoney / Sina / 缓存命中状态，避免误判为“没有行情”。

## P2 - 产品体验与扩展

- [ ] 设置页增加账户画像预设。
  - 现状：支持手动排除创业板 / 科创板 / 北交所。
  - 目标：增加“普通 A 股账户”“已开通创业板”“已开通科创板”等预设。

- [ ] 扫描报告和前端状态建立历史列表。
  - 现状：报告落盘到 `data/reports`，前端只显示当前一次结果路径。
  - 目标：页面可选择历史交易日，查看历史候选、历史证据和历史回测。

- [ ] 持仓 / 自选股导入。
  - 现状：不连接券商，不读取个人持仓。
  - 目标：先支持 CSV 导入中信建投导出的持仓 / 自选股，再考虑券商接口或自动同步。

- [ ] 增加策略解释的“不可买原因”字段。
  - 现状：板块排除后只给剔除数量。
  - 目标：在报告中列出被排除股票、板块原因和是否因账户权限不可交易。
